import:
  - /home/takashabe/.config/alacritty/theme_onedark.yml
  - /home/takashabe/.config/alacritty/font.yml
  - /home/takashabe/.config/alacritty/keybinding.yml

env:
  TERM: xterm-256color

window:
  padding:
    x: 2
    y: 2

mouse:
  double_click: { threshold: 300 }
  triple_click: { threshold: 300 }
  hide_when_typing: true

cursor:
  style: Block
  unfocused_hollow: true

background_opacity: 1.0

shell:
 # TODO: consider path macOS and linux
 program: /usr/bin/fish
 args:
   - --login
